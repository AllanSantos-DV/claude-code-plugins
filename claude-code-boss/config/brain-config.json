{
  "version": 2,
  "description": "Brain Research — pluggable backend (local SQLite or MCP Memory Server) + embedding provider.",
  "backend": {
    "type": "local",
    "mcpMemory": {
      "transport": "stdio",
      "serverUrl": "",
      "runDir": "",
      "projectId": "",
      "jarPath": "",
      "javaArgs": ["-Xmx512m"],
      "downloadUrl": "",
      "expectedSha256": "",
      "timeout": 60000
    },
    "ingestion": {
      "enabled": false,
      "_comment_ingestion": "Quando true E backend.type=mcp-memory, o hook Stop envia a conversa do turno ao memory server (add_document, type=conversation) para curadoria/dreaming server-side. Ative por-usuário no dashboard (grava em DATA_DIR/brain/user-config.json, nunca aqui). Default false: enviar o chat é opt-in explícito (privacidade)."
    }
  },
  "embedder": {
    "provider": "transformers",
    "model": "Xenova/paraphrase-multilingual-MiniLM-L12-v2",
    "dimensions": 384
  },
  "kb": {
    "projectScope": true,
    "maxEntriesPerProject": 10000,
    "archiveAfterDays": 90,
    "retrieval": {
      "fastTopK": 1,
      "deepTopK": 3,
      "minScoreFast": 0.50,
      "minScoreDeep": 0.20,
      "contextExcludeTypes": [],
      "_comment_contextExcludeTypes": "Tipos (lesson/pattern/reference/memory) que NÃO entram no bloco [BRAIN] injetado no UserPromptSubmit; default [] = injeta todos (comportamento atual). Ative por usuário via DATA_DIR/brain/user-config.json (não aqui, pra não afetar terceiros)."
    },
    "rerank": {
      "enabled": true,
      "halfLifeDays": 30,
      "weights": { "relevance": 0.5, "recency": 0.2, "frequency": 0.15, "confidence": 0.15 },
      "citationBoost": { "enabled": true, "alpha": 0.1, "cap": 1.5 }
    },
    "submission": {
      "minBashLines": 3,
      "minOutputChars": 1500
    },
    "skillPromotion": {
      "enabled": true,
      "minRecurrence": 3,
      "minConfidence": 0.8,
      "types": ["lesson", "pattern"]
    },
    "skillCapture": {
      "descriptionMin": 40,
      "descriptionMax": 280,
      "useForMax": 400,
      "doNotUseForMax": 400,
      "_comment_skillCapture": "Limites do capture_lesson type:\"skill\" (validação mecânica em scripts/lib/skill-capture.js). descriptionMax=280 é o mesmo teto que brain-promote.js's legacy truncateDescription() já usava — skills sao always-loaded context, por isso o teto existe. Fora do range -> capture_lesson rejeita (isError), nada e truncado silenciosamente."
    },
    "capture": {
      "maxBlockAttempts": 5,
      "_comment_capture": "Phase 1.5 lesson-capture guard (scripts/capture-dispatch.js). maxBlockAttempts is a SAFETY cap: after this many consecutive Stops where the agent never acked the open capture offer (via capture_lesson/capture_ack), the Stop hook RELENTS and lets the turn end — preventing a hard deadlock when the brain-server/MCP is down (acking is literally impossible) or the model is stuck. Generous on purpose (capture matters, so it re-asks several times first). It does NOT weaken block-until-ack in the normal case, and the un-captured cycle is NEVER dropped — it stays queued for a future session. Override per-user in DATA_DIR/brain/user-config.json under kb.capture, not here (the shipped file is replaced on plugin update)."
    }
  },
  "curation": {
    "maxOutputChars": 1500,
    "maxOutputLines": 30,
    "oneHitMaxRecurrence": 3,
    "oneHitWindowDays": 90
  }
}
